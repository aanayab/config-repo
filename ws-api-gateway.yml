server:
  servlet:
    context-path: ''
spring:
  config:
    import: endpoints.yml
#  webflux:
#    base-path: /ws-api-gateway
management:
  endpoint:
    health:
      enabled: true
      show-details: always
    shutdown:
      enabled: true
    pause:
      enabled: true
    restart:
      enabled: true
    refresh:
      enabled: true
  info:
    env:
      enabled: true
    build:
      enabled: true
    git:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
  endpoints:
    web:
#      base-path: /actuator
      exposure:
        include: '*'
